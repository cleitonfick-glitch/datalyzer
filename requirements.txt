# DataLyzer — Dependências
# Instale com: pip install -r requirements.txt

# Interface
streamlit>=1.32.0

# Manipulação de dados
pandas>=2.0.0
numpy>=1.24.0
openpyxl>=3.1.0          # Leitura de .xlsx
xlrd>=2.0.1               # Leitura de .xls (formato antigo)

# PDF
pdfplumber>=0.10.0        # Extração de tabelas (preferido)
PyPDF2>=3.0.0             # Fallback para extração de texto

# Word
python-docx>=1.1.0

# Visualização
plotly>=5.19.0
matplotlib>=3.8.0         # Utilitário complementar
seaborn>=0.13.0           # Utilitário complementar

# Machine Learning (análises opcionais)
scikit-learn>=1.3.0

# Exportação de gráficos (PNG/PDF)
kaleido>=0.2.1            # Necessário para fig.to_image()
